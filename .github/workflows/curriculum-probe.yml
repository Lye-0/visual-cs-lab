name: Curriculum model probe
on:
  push:
    branches: [feat/curriculum-gap-expansion]
    paths:
      - 'src/**'
      - 'scripts/**'
      - 'tests/**'
      - '.github/workflows/curriculum-probe.yml'
permissions:
  contents: read
concurrency:
  group: curriculum-probe-${{ github.ref }}
  cancel-in-progress: true
jobs:
  models:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - name: Check syntax, model bounds and independent numerical examples
        run: node scripts/curriculum-probe.mjs
      - name: Archive the exact source used by this check
        if: always()
        run: |
          mkdir -p review-output
          git archive --format=zip --prefix=visual-cs-lab/ HEAD -o review-output/curriculum-source.zip
      - uses: actions/upload-artifact@v5
        if: always()
        with:
          name: curriculum-model-probe
          path: review-output/
          retention-days: 14
