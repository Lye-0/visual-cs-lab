# AI・自然言語処理教材の検証記録

検証したコミット：`5457373fcdeed0a8aacb917376e5679618edf7e0`。
ブランチ：`feat/lesson-specific-experiences`。
対象はGAP-122〜132の11単元・30章と専用12操作です。この記録は、全314単元の教え方・目視レビューの完成やmainへの反映を意味しません。実装内容と残す制約は [EXPERIENCE_AI_LANGUAGE.md](../EXPERIENCE_AI_LANGUAGE.md) を参照してください。

## 専用検証の結果

[AI and language learning workspaces / run 35682494117](https://github.com/Lye-0/visual-cs-lab/actions/runs/35682494117) の3ジョブで、checkoutしたSHA、モデルとブラウザの結果、検査後のsource diffを確認しました。

| 検証 | 成功 | 失敗 | ジョブID |
|---|---:|---:|---|
| Node.js専用モデル検査 | 35 | 0 | 各ブラウザジョブで同じ検査を実行 |
| Chromium 153.0.8010.12 | 129 | 0 | 106602233999 |
| Firefox 155.0 | 129 | 0 | 106602234126 |
| WebKit 26.6 | 129 | 0 | 106602234153 |

ブラウザの129件は、30章の描画・入力ラベル等を3画面幅で調べる90件と、各幅13の操作シナリオ39件です。幅1440・390・320px、高さ960px。小さい画面のコンテキストでタッチ対応を有効にしていますが、スマートフォン実機での検証ではありません。ランタイム例外は各ブラウザとも空でした。

各章で描画の完了、入力のlabel、数値入力の初期値、ページ全体の横はみ出し、CSP違反を確認します。直接操作の検査では、DOMをクリックして結果を調べ、期待する出力へ画面を差し替えません。コミットされた外部JS/CSSをローカルHTTP配信し、検査中のビルドやソースの書換えは行いません。

### 独立した期待値・反例の検査

- 前処理：train6行のage補完値30・income補完値22、標準偏差、中央値、定数列を数値で照合。testだけの編集でfit値が変わらず、train編集や役割変更では古いfitが無効になることを確認します。
- 分類：問い合わせ点の変更で学習集合が変わらないこと、線形scoreとsigmoidの関係、木の条件に従った経路を確認します。
- 選択：候補確定前のtest開示を拒否し、開示後の再選択を独立評価に戻しません。test誤差は保存した係数と固定testから独立に計算して照合します。
- 逆伝播：選んだ重みの連鎖律の4例平均と、損失を実際に差分計算した有限差分を照合。一回の重み更新で旧値−学習率×勾配になることも確認します。
- A*：本人が選んだ候補を規則と照合。最終費用は別のBellman-Ford計算で求めた基準と比較し、不適切な選択で元の状態が変わらないことを検査します。
- Q学習：行動だけではQを変更しないこと、終端では未来のQを使わないこと、方向がずれた場合も選択した行動のQへ反映することを確認します。
- 分割：固定文の標準分割の費用5、通/信に分けた費用13、品詞の遷移費用を除いた10を照合。辞書にない複数文字の語を勝手に有効扱いしません。
- 文書：networkのdf=2とidf=ln(5/3)+1、語順の入替え、未知語だけのゼロベクトル、内積の各項を照合します。
- 係り受け：I・see・birdsの三つの辺、ROOTが一つ、各語のheadが一つという条件と遷移の前提を確認します。
- Attention：Queryが0なら重み1/2ずつ、出力[1,2]。Valueを変えても重みを変えず、mask後は後ろのValueを混ぜません。
- 評価：混同行列[[2,0],[1,1]]のセルに元の事例が対応し、評価文を変更しても学習文を変えないことを確認します。正解関連集合の変更でも検索順位は保持します。

### 実際のUI操作で確認したこと

前処理のfit/applyの分離、分類図のクリック直後の矢印キー、test開示後にリセットしても同じ画面で警告が残ること、逆伝播後の実際の重みと編集欄の一致、A*の誤選択の拒否、Qの方向ずれ、単語境界のEnter操作とフォーカス保持、未知語とHTMLに見える文字列の無害な表示、係り受けの組立て、Q/K/Vの独立編集、混同行列の事例確認、検索のkと正解関連の変更、入力保持・取消し・リセット・画面移動を検査しました。

公開用追加ソースに外部通信・永続保存・任意JavaScript実行を追加しないことも検査しています。操作履歴は現在の実験の一操作取消し用で、学習履歴の保存機能ではありません。

## 接続時に修正した問題

一回限りの入口接続ジョブの最初の実行では、Q更新の式表示に閉じ括弧が不足していたため構文検査で停止しました。成功したように扱わず、ソースを修正してから入口を生成しました。

重みの入力欄は、選択unit・feature・更新回数を含む識別子へ変更しました。これにより、入力保持の仕組みが更新前の重みを新しい画面へ戻すことを防ぎ、学習率などの利用者の入力は別に保持します。分類図のクリックと矢印キーは、再描画後のSVGへフォーカスを戻す実装です。

これらは `503cccddbd99005ae8be62009a433138215ac261` でソース・読み込み一覧・入口HTMLへ反映済みです。一回限りの書込みワークフローは自分自身を削除してからコミットされました。今回検証した `5457373...` は、そのコードに実装範囲の文書を加えたコミットです。恒久CIが検査時に教材を生成して成功へ置き換える構成にはしていません。

## 以前の範囲の回帰確認

同じ `5457373fcdeed0a8aacb917376e5679618edf7e0` で、以下のワークフローがcompleted/successでした。

| ワークフロー | run ID | 結果 |
|---|---|---|
| Direct learning workspaces | [35682494147](https://github.com/Lye-0/visual-cs-lab/actions/runs/35682494147) | 成功 |
| OS and data learning workspaces | [35682494087](https://github.com/Lye-0/visual-cs-lab/actions/runs/35682494087) | 成功 |
| Network learning workspaces | [35682494031](https://github.com/Lye-0/visual-cs-lab/actions/runs/35682494031) | 成功 |
| Security learning workspaces | [35682494172](https://github.com/Lye-0/visual-cs-lab/actions/runs/35682494172) | 成功 |

## 全314単元の完了検査は未完了

[Lesson-specific experience review / run 35682494036](https://github.com/Lye-0/visual-cs-lab/actions/runs/35682494036)、job `106602238169` の全ログを確認しました。

| 全体検査 | 成功 | 失敗 | 原因 |
|---|---:|---:|---|
| `npm run check` | 3,684 | 1 | 全314単元の明示定義を要求する検査で291≠314 |
| 新教材の全体ブラウザ検査 | 1,040 | 48 | GAP-133〜155の23単元×PC/モバイル＝46件、総数確認2件 |

ブラウザの例外一覧は空でした。この検査実行における残りの失敗は欠落確認です。だからといって、検査していない操作や目視の品質まで全て正しいとは判断しません。

登録数は **291単元・509章・524活動**、既存159単元とGAP-001〜132です。新しい画面が未登録なのは **GAP-133〜155の23単元**。全314を求めるassertionを下げたり、欠落単元へ名前だけの定義を生成したりしていません。

## 未検証・今後の仕上げ

スクリーンショットはCI artifactへ生成済みですが、この実装セッションでは実行環境の接続エラーで画像を展開・目視できていません。PNGを取得したことや、横はみ出しの自動検査を通したことを、目視レビュー完了とは表現しません。

学習者による分かりやすさの評価、スクリーンリーダーの全操作、Safari・スマートフォン実機は未検証です。登録済みのページも含めて、最初の個別計画との照合と仕上げが必要です。minimax/Queen配置の自由操作や、生成文の語対応を選ぶ追加画面など、今回実装していない項目は実装範囲文書に残しています。mainへはまだマージしていません。

## 再実行

```powershell
node scripts/build.mjs --check
node --test tests/ai-workspaces.test.mjs
npm install --no-save --package-lock=false --ignore-scripts playwright@1.63.0
npx playwright install chromium firefox webkit
$env:BROWSER = "chromium"
node tests/ai-workspaces-browser.mjs
$env:BROWSER = "firefox"
node tests/ai-workspaces-browser.mjs
$env:BROWSER = "webkit"
node tests/ai-workspaces-browser.mjs
```

CIのLinux環境ではbrowser installへ `--with-deps` を付けます。サイトの起動・公開にはPlaywrightを必要としません。結果は `review-output/ai-<engine>.json`、PNGは `review-output/ai-screenshots/` です。
