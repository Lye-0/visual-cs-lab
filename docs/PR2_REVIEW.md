# PR #2: Copilotレビューの判断と対応

レビュー対象: `5d5a190d8ab34ee716609b962a4f290df95e8578`。通常コメント5件とレビュー本文内のSuppressed comments 2件、合計7件を対象にしています。Copilotの指摘は判断材料であり、そのまま正解とは扱いません。

| ID | 対象 | 判断 | 採用・代案 |
|---|---|---|---|
| CP-01 | CIがソースを修復・commitしてから検証する | 設計への指摘は妥当。対象headに構文エラーが残るという部分は別途検証する | 継続的な修復・pushを廃止。headを固定して読み取り専用で検証。Lesson reviewはマージ予定結果も検証する。 |
| CP-02 | 古いcurriculum-status.json | 妥当 | 削除案を採用。現在の合否は対象SHAのChecks/Artifacts。過去のレポートはsourceCommitを明記した履歴として区別。 |
| CP-03 | 引数型エラー時に呼出し環境が漏れる | 妥当 | 型検査をスタック変更前に実施。引数の束縛・図の生成・本体実行もfinallyによる後始末の範囲へ置く。 |
| CP-04 | schedulerの完了・I/Oが1tick早い | 現行コードでは不妥当 | tick()内でtime++した後にfinished=time、wake=time+ioを設定している。+1すると逆に1tick遅れるため採用しない。独立した時系列例で固定する。 |
| CP-05 | SVGのrole=buttonがEnter/Spaceで操作できない | 妥当 | SVGに限定したキー操作から既存の選択処理へ委譲。再描画後のフォーカス、Spaceで再生されないこと、押しっぱなしの重複操作も確認。 |
| CP-06 | migrationのincludes判定が修正を見逃す（折り畳み） | 適用判定として問題がある | 判定式の継ぎ足しでなく、CP-01と合わせて移行自体と全patch JSONを廃止。 |
| CP-07 | 動的配列の初期容量を黙って引き上げる（折り畳み） | 初期状態の不整合を隠す点は妥当。ただし現行の挿入処理は容量一致時に倍増するので「6要素/容量5」は再現しない | 過容量の初期状態を明示的に拒否する提案を採用。正当な挿入の倍増とコピー回数も検査する。 |

## 既存のPR用ブラウザテスト

単純なHTML文字列一致のうち、DOM教材が再描画ごとに付ける一意IDだけを構造的に正規化します。表示文・入力の現在値・checked/selected・disabled・ラベルと入力の対応・図の構造は比較を残します。重複IDや壊れた参照は正規化前にエラーにし、文字列全体に対する無差別な置換で隠しません。

## 検証

検証結果はこの文書の作成時点では確定していません。最終結果は同じPRのChecksにある対象SHAと、その実行のArtifactsから確認します。マージ、承認、レビューの自動dismissは行いません。
