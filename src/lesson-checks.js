/* Concrete questions for the new explanations, not one repeated generic quiz. */
(() => {
'use strict';const L=CSL;
const questions={
'c01-information':['確率を1/8から1/2に変えると、自己情報量は？','3bitから1bitへ減る','1bitから3bitへ増える','出来事の名前が同じなので変わらない','−log₂(1/8)=3、−log₂(1/2)=1です。起きやすい出来事ほど、知ったときの情報量は小さくなります。'],
'c01-entropy':['平均情報量を求めるには？','各情報量に、その確率を掛けて足す','自己情報量を単に4で割る','最も珍しい出来事の値だけを使う','出やすさの異なる出来事を、確率で重み付けします。1/2×1+1/4×2+1/8×3+1/8×3=1.75です。'],
'c01-joint':['Yを知った後に残るXの不確かさは？','H(X,Y)−H(Y)','H(X)+H(Y)をそのまま足した値','常に0','全体の情報量から、すでに知ったYの量を引きます。独立ならYを知ってもXの不確かさは減りません。'],
'c01-markov':['各状態のエントロピーを重み付けするのは？','各状態にいる定常確率','状態番号そのもの','必ず同じ重み1/2','遷移の行を作る確率と、その状態に滞在する確率は別です。π₀H₀+π₁H₁を計算します。'],
'c01-channel':['同じBSCで入力の確率を変えると？','伝送情報量は変わり得るが、通信路容量は同じ','通信路容量も入力の確率と必ず同じ値になる','反転する確率εが自動で変わる','容量は入力分布について最大化した量です。通信路を固定して入力だけ変えたときのIと区別します。'],
'c01-prefix':['接頭辞の重なりがあると分かったら？','瞬時符号ではないが、一意性は別に調べる','必ず一意的復号も不可能と断定できる','受信列を何でも復号できる','一意的復号と瞬時復号を区別します。2通りの元の記号列が同じビット列になる反例が見つかれば、一意性の不成立を示せます。'],
'c01-kraft':['クラフトの不等式が示すのは？','指定した長さで瞬時符号を作れるか','適当に選んだ符号語がすべて正しいこと','どの圧縮でも必ず同じ符号になること','長さの組としての存在条件です。実際に割り当てた符号語の接頭辞条件は別に確認します。'],
'c01-shannon-fano':['この符号木の作り方は？','確率の和が近い2群へ分けていく','最も小さい2つを常に下から結合する','記号をランダムに分割する','小さい2つをまとめるのはHuffmanの手順です。Shannon–Fanoでは上から分割を繰り返します。'],
'c01-distance':['ハミング距離を求めるには？','XORの結果に含まれる1を数える','ビット列を整数に直して引く','1の総数だけをそれぞれ比べる','同じ位置の違いを数えます。違う位置を選んでも距離が同じになることがあります。'],
'c01-linear-code':['1ビット誤りでシンドロームがHの第2列と同じなら？','受信列の第2ビットを反転して確かめる','第2情報ビットを必ず削除する','正解の入力を参照して復元する','誤り位置を判定する情報は受信列からの検査結果です。1ビット以下という対象範囲を外すと、この保証は使えません。'],
'c02-nfa':['集合状態{q0,q2}とは？','q0にいる経路とq2にいる経路の両方が可能','半分の確率でどちらかを選んだ状態','q0を消してq2だけに決めた状態','可能な経路を全部表します。入力を読み終えた時点で受理状態を含めば、受理する経路が存在します。'],
'c08-nand':['A NAND Aは何になる？','NOT A','常にAと同じ','常に1','A AND AはAなので、NANDではその否定です。A=0とA=1の両方で確かめられます。'],
'c08-register':['load=0でクロックが来ると？','前のQを選んで取り込み、値を保持する','新しいDを必ず取り込む','回路からクロック自体が消える','Muxが保持中のQを選び、その値をDFFが取り込みます。選択とタイミングを区別します。'],
'c10-address-spaces':['アプリAとBで同じ仮想番地を使うと？','別のページ表なら別の物理番地へ対応できる','必ず同じ物理番地を上書きする','数値が同じなので片方は実行できない','仮想番地だけでなく、どのプロセスの変換表を使うかを確認します。この例は別々のフレームへ対応させています。']
};
for(const [id,q]of Object.entries(questions)){const l=L.labs.find(x=>x.id===id);if(!l)throw Error('確認問題の単元が見つかりません: '+id);const rotation=id.length%3,options=q.slice(1,4),ordered=options.slice(rotation).concat(options.slice(0,rotation));l.challenge={question:q[0],options:ordered,answer:ordered.indexOf(q[1]),explanation:q[4]};}
for(const c of L.readingCourses){const entry=L.courses.find(x=>x.name===c.name);if(entry)entry.labs=[...new Set(c.groups.flatMap(x=>x[1]))];}
// Use local, non-private reference links in the legacy source directory too.
L.sources.notesInfo.url='#/course/information';L.sources.notesIntro.url='#/course/introduction';
})();
