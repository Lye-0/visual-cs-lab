{
  "name": "visual-cs-lab",
  "version": "2.0.0",
  "private": true,
  "description": "オフラインで動く情報科学・セキュリティ・ネットワークのインタラクティブ実験室",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node scripts/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "inventory": "node scripts/inventory.mjs",
    "check": "npm test && npm run build && npm run inventory"
  },
  "engines": {
    "node": ">=20"
  }
}
